const styles = theme => ({
    footer: {
        backgroundColor: theme.palette.background.paper,
        position: "fixed",
        bottom: 0,
        margin: "auto",
        left: "50%",
        transform: "translateX(-50%)"
    },
});

export default styles;